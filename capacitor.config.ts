import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'academyVoice',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
